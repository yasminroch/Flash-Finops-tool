# Flash Analytics Frontend
# Em dev: deixa vazio (usa proxy do vite pra localhost:8000)
# Em prod (Render): coloca a URL completa do backend
VITE_API_URL=
# Exemplo prod: VITE_API_URL=https://flash-analytics-api.onrender.com/api
